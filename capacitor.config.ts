import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.b3nk4n.faqapp',
  appName: 'ionic-faq-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
